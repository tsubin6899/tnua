# 招生統計中央模式操作說明

中央模式把原本只存在瀏覽器內的編輯內容，改存到同一台伺服器，並提供登入、權限、草稿、正式發布與版本回復。直接開啟 `index.html` 時仍可使用原本的單機模式；透過中央伺服器網址開啟時，才會啟用中央模式。

## 1. 快速啟動

在 PowerShell 進入本資料夾後執行：

```powershell
.\啟動中央模式.ps1
```

開啟 `http://127.0.0.1:4178`。若未設定帳號密碼，啟動視窗會顯示本次的隨機管理者密碼；帳號為 `admin`。重新啟動後隨機密碼會改變，所以正式使用前請依下節設定固定帳號。

中央資料預設保存在 `server-data`，此資料夾已排除於 Git 版本控制之外。停止服務前，請確認頁面顯示「草稿已儲存」或「已正式發布」。

## 2. 設定固定帳號與工作階段金鑰

請在啟動服務前設定環境變數。以下範例建立三種角色：

```powershell
$env:TNUA_USERS_JSON = '{"admission_admin":{"name":"招生組管理者","password":"請改成高強度密碼","role":"admin"},"admission_editor":{"name":"資料維護人員","password":"請改成另一組高強度密碼","role":"editor"},"admission_viewer":{"name":"主管檢視","password":"請改成第三組高強度密碼","role":"viewer"}}'
$env:TNUA_SESSION_SECRET = '請改成至少 32 個隨機字元且妥善保存'
.\啟動中央模式.ps1
```

權限說明：

- `viewer`：只能查看已正式發布的版本。
- `editor`：可匯入、修正並儲存共同草稿，但不能正式發布。
- `admin`：可儲存草稿、正式發布、查看版本並回復舊版本。

正式使用時不要把密碼或 `TNUA_SESSION_SECRET` 寫入 Git、HTML 或共用文件。

## 3. 建議操作流程

1. 編輯者登入後匯入 Excel 或修正資料。
2. 先檢查「資料狀態」是否正確：已完成、進行中或預估。
3. 確認空白欄位顯示「待補」，不要手動填入 0 代表未知。
4. 按「儲存共同草稿」。若其他人已先更新，系統會阻止覆蓋並要求重新載入。
5. 管理者複核後按「正式發布」。檢視者只會看到這個正式版本。
6. 如需回復，管理者可在版本紀錄選擇舊版本；回復動作本身也會留下新版本與稽核紀錄。

## 4. 內網多人使用

可用下列指令監聽所有網路介面：

```powershell
.\啟動中央模式.ps1 -HostAddress 0.0.0.0 -Port 4178
```

同仁再以該電腦的內網 IP 開啟網站。若跨電腦使用，建議由資訊單位配置 HTTPS 反向代理、防火牆白名單、固定 DNS、備份與服務自動啟動；不要把未加密的 HTTP 服務直接暴露到網際網路。

## 5. 備份與復原

定期備份整個 `server-data` 資料夾，至少包含：

- `state.json`：目前草稿、正式發布內容、版本索引與稽核紀錄。
- `versions`：每次儲存、發布或回復的版本快照。

復原時先停止服務，以完整備份覆蓋 `server-data` 後再重新啟動。中央服務預設保留最近 50 個版本與 500 筆稽核紀錄。

